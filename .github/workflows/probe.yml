name: probe
on:
  push:
    branches: [main]
jobs:
  gated:
    runs-on: ubuntu-latest
    environment: probe
    concurrency:
      group: probe
      cancel-in-progress: true
    steps:
      - run: echo "ran ${{ github.run_id }}"
